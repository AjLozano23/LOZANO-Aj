// object car with the properties type, model, and color.
let car = {
    type: "Sedan",
    model: "Camry",
    color: "Red"
};

console.log(typeof car); // typeof to check the type of the object

car.type = "Toyota"; //Update the type property to Toyota
car.wheels = 4; // new property wheels with the value 4
console.log(car);